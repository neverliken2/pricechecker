# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME_PREFIX=smlerpmain

# Connection Settings
DB_CONNECTION_TIMEOUT=10000

# Cookie Settings (set to false for development or HTTP)
COOKIE_SECURE=false
